/*
 * Javascript for jTime
 */

$(document).ready(function() {

	// sort out the datepicker
	$('input.datepicker').Zebra_DatePicker({
		direction: false,
		format: 'd/m/Y'
	});

	// TODO: set date to yesterday by default
 });
